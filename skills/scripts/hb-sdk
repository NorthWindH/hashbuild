#!/usr/bin/env python3
#
# /// script
# requires-python = ">=3.10"
# dependencies = []
# ///

"""hb-sdk — Hashbuild SDK CLI entry point."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))
from hb_sdk.__main__ import main

main()
