# Step 5 Execution — Push ticket(s) action

**Executed:** 2026-07-16T15-04-15-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** *pending — committed in the same commit as this file, per this skill's own Step 8*
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

`hb-ticket-discuss`'s loop now recognizes "push the active ticket," "push
PROJ-123 and PROJ-124," and "push all tickets" — none of which were
recognized before. The new **Push ticket(s)** action resolves any subset of
in-context tickets and walks each one through the existing Jira
create/update/Idea-link flow, reusing the pre-restructuring push procedure
(recovered from `7bd2c42`) unchanged in substance. Scope held to additive
only: one new subflow file, three prose edits, no new `.hb/` write, no
`allowed-tools` widening. A pushed ticket is never removed from context
(AC5). No automated build/test gate exists for this markdown-procedure repo;
all structural/grep checks below are green.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/push-ticket-subflow.md` | **new** — full Push action subflow: §A resolve target set, §B per-ticket push procedure (steps 1-6, recovered `7bd2c42` Steps 3-7 verbatim in substance, `$TARGET.content` replacing `$WRITTEN_TICKET`), §C multi-target sequential loop with continue/stop checkpoint, §D compose outcome |
| `skills/references/ticket-loop-subflow.md` | **edit** — one new Action Registry row (`Push ticket(s)` → `push-ticket-subflow.md`) between Clear and Exit; §A, §C, §D, §E untouched |
| `skills/hb-ticket-discuss.md` | **edit** — `description:` and body prose: "clear, exit" → "clear, push, exit" (2 hunks); `allowed-tools` block byte-unchanged (still the 5 read-only Jira tools; the 3 mutating tools intentionally excluded — see plan's Design decision) |
| `skills/references/references-toc.md` | **edit** — one new row for `push-ticket-subflow.md`, between Clear's and Exit's rows |

No dependency manifest or lockfile in this repo's skill layer — none touched.

`.hb/facts.md` was **not** edited: the plan named a fact to prune ("re-author
Jira push logic deleted") but no such fact exists in the current store (see
Deviations).

---

## Deviations from plan

- **Plan §4 listed `.hb/facts.md` as an edit** to prune a "re-author Jira
  push logic deleted" fact. `grep -i "push|recoverable|deleted"
  .hb/facts.md` and a full read of the file found no such fact — it was
  never added, or was already removed. No edit was made; this is a stale
  plan detail, consistent with the existing fact that this task's step-2
  plan.md was already found stale against live files. Result is still
  correct: the facts store has nothing to prune.
- Everything else followed the plan as written — subflow shape, Action
  Registry position, `allowed-tools` non-change, and the recovered
  push-procedure content all match §2/§3 of the plan.

None of substance beyond the above.

---

## Verification

| Check | Result |
|---|---|
| Build | N/A — markdown-procedure repo, no build |
| Full test suite | N/A — no automated harness |
| Static analysis / lint | N/A |
| Formatter | N/A |
| End-to-end run / artifact regeneration | N/A |
| Scope check (only intended files changed) | confirmed — see below |

Per-AC / structural checks, actual output:

```
$ grep -n "Push ticket(s)" skills/references/ticket-loop-subflow.md
40:| Push ticket(s) | "push this", "push PROJ-123 and PROJ-124", "push all tickets" | `push-ticket-subflow.md` |

$ grep -c "^####" skills/references/push-ticket-subflow.md
4

$ grep -n "mcp__claude_ai_Atlassian_Rovo__" skills/hb-ticket-discuss.md | wc -l
       5

$ sed -n '/^allowed-tools/,/^---$/p' skills/hb-ticket-discuss.md | grep -n "createJiraIssue\|editJiraIssue\|createIssueLink"
(no output — mutating tools correctly excluded)

$ grep -n "clear, push, exit" skills/hb-ticket-discuss.md
10:  actions (e.g. describe, load, breakdown, clear, push, exit) selectable via
28:...menu of next actions (e.g. describe, load, breakdown, clear, push, exit) selectable via natural language.

$ grep -n "push-ticket-subflow" skills/references/references-toc.md
20:| `${CLAUDE_SKILL_DIR}/references/push-ticket-subflow.md` | ... |

$ grep -n "Write(\|Edit(\|^Bash" skills/references/push-ticket-subflow.md
(no output — no file-write calls in the subflow's prose)

$ git diff --stat -- skills/references/breakdown-subflow.md skills/references/load-ticket-subflow.md \
    skills/references/describe-ticket-subflow.md skills/references/breakdown-ticket-subflow.md \
    skills/references/exit-ticket-loop-subflow.md skills/references/clear-ticket-subflow.md
 skills/references/breakdown-subflow.md | 1 +
 1 file changed, 1 insertion(+)
```

The one line on `breakdown-subflow.md` is a pre-existing uncommitted
`TODO REVIEW` comment present in the working tree **before** this step
started (confirmed via the session's initial `git status`) — untouched by
this execution. The other five sibling subflows show zero diff, confirming
non-regression.

```
$ git diff -- skills/hb-ticket-discuss.md
```
Shows exactly the two wording hunks (description + body prose); the
`allowed-tools` block is byte-identical to before.

```
$ git diff -- skills/references/ticket-loop-subflow.md
```
Shows exactly the one new Action Registry row; §A, §C, §D, §E unchanged.

```
$ git diff -- skills/references/references-toc.md
```
Shows exactly one new row, positioned between Clear's and Exit's rows.

`push-ticket-subflow.md` opens with the required blockquote + "Caller
contract." and ends with a "Failure/degradation contract" paragraph (both
confirmed by reading the file's head/tail).

---

## Acceptance-criteria status

All of AC1-AC7 satisfied, per the plan's §7 traceability table (unchanged —
the implementation followed that table exactly):

- AC1/1.1/1.2/1.3 — Push action added to the menu; bare/single/active
  default, several named tickets, and "all tickets" all resolve per §A.
- AC2.1-2.4 — Jira MCP detection, NL resolution/confirmation, push +
  key/URL reporting, and Epic→Idea-link offer are recovered verbatim in
  substance from `7bd2c42`, per §B.
- AC3 — sequential, never concurrent; explicit continue/stop checkpoint
  between targets, per §C.
- AC4 — per-ticket outcome tracked (`pushResult`) and summarized, per §B
  step 6 / §D.
- AC5 — a pushed ticket stays in context; Push never adds/removes/
  reactivates a `$TICKET_CONTEXT` entry.
- AC6 — logic lives in its own new subflow file; TOC updated.
- AC7 — deliberate, documented deviation: `allowed-tools` keeps its 5
  read-only Jira tools; the 3 mutating tools are intentionally excluded so
  every Jira write still prompts the user for consent (plan's Design
  decision, carried through unchanged).

---

## Tests added

None — no automated test harness exists in this markdown-procedure repo.
Coverage is the structural/grep checks above plus the dry-run traces the
plan's §5 lists (single-ticket default, active-ticket default, ambiguous
bare "push," multiple named targets, "push all," stop mid-batch, no-MCP
fallback, decline/failure fall-through, Epic Idea-link accept/decline/fail,
non-Epic no-prompt, ticket-remains-in-context, and Load/Describe/Breakdown/
Clear/Exit non-regression) — these are exercised live via `/hb-ticket-discuss`
rather than an automated suite, consistent with steps 1-4's own convention.

---

## Honest notes (optional)

- The plan's claim that `.hb/facts.md` needed a prune was not borne out by
  the live facts store — see Deviations. Nothing else in the plan was
  stale.
- This subflow's per-ticket push logic (§B) is long by necessity: it
  reproduces the recovered pre-restructuring procedure's five steps
  (detect/describe, resolve/confirm, push, Idea-link, emit-fallback) almost
  verbatim so that AC2's "unchanged in substance" requirement holds. It was
  not trimmed further, since doing so would risk silently changing behavior
  that AC2 requires to stay identical.

---

## Follow-up raised / Deferred / Out of scope

- Out of scope per ticket: no new MCP/source integrations, no change to
  Idea-link direction/semantics, no concurrent/parallel pushing — all
  carried forward unchanged, per plan §8.
- This task (`northwind/hb-015-ticket-breakdown`) still has steps 0, 1, 3,
  and 4 awaiting review (`/hb-task-step-review-address`) — unrelated to this
  step's own scope, flagged only for the task's own next-action tracking.
