# Step 4 Execution — Clear ticket(s) action

**Executed:** 2026-07-16T06-28-18-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending (committed immediately after this file is written) — *northwind/hb-015-ticket-breakdown/step-4: (step-execute) add clear-ticket(s) action*
**Result:** ✅ Complete — all acceptance criteria met, gates green (structural)

---

## Outcome

The motivating case is resolved: a user accumulating several tickets across an `hb-ticket-discuss` session can now say "clear the active ticket", "remove PROJ-123 and PROJ-124", or "clear all tickets" at any loop iteration, and exactly the named ticket(s) are removed from `$TICKET_CONTEXT` — without having to `/clear` the whole conversation and lose every other ticket in context. General result: the Action Registry grows `4 rows → 5 rows` (Load, Describe, Breakdown, Clear, Exit), via one new subflow file (`clear-ticket-subflow.md`, 68 lines, 3 lettered sections) plus one new Action Registry row. Scope boundary held: additive only, in-memory context mutation only — no file I/O, no new tool grant, no `.hb/` writes. All four existing actions (Load, Describe, Breakdown, Exit) and their subflow files are byte-unchanged.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/clear-ticket-subflow.md` | **new** — full Clear ticket(s) action subflow: §A resolve target set (empty-context / "all" / "active ticket" / named-reference(s), never-guess posture matching `breakdown-ticket-subflow.md` §A), §B confirm (count-based gate, `\|$TARGETS\| > 1`), §C apply (remove, no auto-promotion, compose outcome string) |
| `skills/references/ticket-loop-subflow.md` | **edit** — one new Action Registry row (§B), inserted between Breakdown and Exit; Sections A, C, D, E untouched |
| `skills/hb-ticket-discuss.md` | **edit** — `description:` frontmatter and body-prose example-actions list both gain "clear" (two wording hunks only); `allowed-tools` and `Steps` section untouched |
| `skills/references/references-toc.md` | **edit** — one new row for `clear-ticket-subflow.md`, between the `breakdown-ticket-subflow.md` and `exit-ticket-loop-subflow.md` rows |

No dependency manifest or lockfile in this repo's skill layer; none touched.

**Note:** `skills/references/breakdown-subflow.md` shows as modified in `git status` (a pre-existing, uncommitted `TODO REVIEW` comment predating this step's work — confirmed via `git diff`, unrelated to Clear). It is deliberately excluded from this step's staged changes and commit.

---

## Deviations from plan

None of substance. Implementation matches plan §2/§3/§4 exactly — same file list, same section shape, same algorithm (target-set resolution → count-based confirm → apply with no auto-promotion).

---

## Verification

| Check | Result |
|---|---|
| Build | N/A — markdown-procedure repo, no build step |
| Full test suite | N/A — no automated test harness (confirmed by every prior step-0 through step-3 execution summary in this task) |
| Static analysis / lint | N/A — no linter configured for skill markdown |
| Formatter | N/A |
| End-to-end run / artifact regeneration | N/A — no artifact regeneration step |
| Scope check (only intended files changed) | confirmed — see below |

**Per-plan structural checks (§5/§6), actual output:**

```
$ grep -n "Clear ticket(s)" skills/references/ticket-loop-subflow.md
39:| Clear ticket(s) | "clear the active ticket", "remove PROJ-123 and PROJ-124", "clear all tickets" | `clear-ticket-subflow.md` |
```
Exactly one row, positioned between Breakdown and Exit; the other three rows' text unchanged (confirmed by full-file read).

```
$ grep -c "^####" skills/references/clear-ticket-subflow.md
3
```
Exactly 3 lettered sections (§A-C), matching sibling subflow shape.

```
$ git diff --stat -- skills/references/load-ticket-subflow.md \
    skills/references/describe-ticket-subflow.md \
    skills/references/breakdown-ticket-subflow.md \
    skills/references/exit-ticket-loop-subflow.md \
    skills/references/breakdown-subflow.md
 skills/references/breakdown-subflow.md | 1 +
 1 file changed, 1 insertion(+)
```
Load/Describe/Breakdown/Exit subflows: zero changes. `breakdown-subflow.md`'s single-line diff is the pre-existing, uncommitted `TODO REVIEW` comment noted above — not introduced by this step, and not staged in this step's commit.

```
$ git diff -- skills/hb-ticket-discuss.md
```
Shows only the two wording hunks (`description:` and body prose, each "...breakdown, exit)" → "...breakdown, clear, exit)"). No hunk touches the `allowed-tools:` block or the `Steps` section.

```
$ grep -n "clear-ticket-subflow" skills/references/references-toc.md
19:| `${CLAUDE_SKILL_DIR}/references/clear-ticket-subflow.md`      | Clear-ticket(s) action for `hb-ticket-discuss`'s loop; dispatched from `ticket-loop-subflow.md`'s Action Registry.                         |
```
Exactly one row, positioned between the `breakdown-ticket-subflow.md` and `exit-ticket-loop-subflow.md` rows.

```
$ grep -n "^Bash\|hb-sdk" skills/references/clear-ticket-subflow.md
(no output)
$ grep -n "Write\|mkdir\|TARGET_PATH" skills/references/clear-ticket-subflow.md
(no output)
```
Reuse guard and no-file-write guard both clean — pure conversational state mutation, no SDK/shell/file side effects inlined.

**Per-AC checks (§6 step 6), actual output:**

```
$ grep -n "never auto-select" skills/references/clear-ticket-subflow.md
33:        picks one — never auto-select.
$ grep -n "\$TARGETS.*>.*1" skills/references/clear-ticket-subflow.md
43:1. `|$TARGETS|` > 1 (true for any multi-entry "all" or multi-name request) →
$ grep -n "no auto-promotion\|Do not set" skills/references/clear-ticket-subflow.md
54:2. Do not set `active: true` on any remaining entry, even if the removed
55:   set included the previously-active one — no auto-promotion. ...
```
Confirms: §A never auto-selects on an ambiguous or zero-match reference; §B's confirm gate is stated as count-based (`|$TARGETS| > 1`), not phrasing-based; §C explicitly states no auto-promotion of a new active entry.

```
$ git status --short
 M skills/hb-ticket-discuss.md
 M skills/references/breakdown-subflow.md
 M skills/references/references-toc.md
 M skills/references/ticket-loop-subflow.md
?? skills/references/clear-ticket-subflow.md
```
Matches plan §4's file list exactly, plus the pre-existing unrelated `breakdown-subflow.md` line (excluded from this step's commit) and this step's own `.hb/task/...` artifacts (execution summary, added separately to the commit).

---

## Acceptance-criteria status

All 7 ACs satisfied per the plan's traceability table (plan §7), unchanged from the plan's mapping since implementation followed it exactly:

| AC | Satisfied by |
|---|---|
| 1 — Clear ticket(s) action added to menu, NL-selectable | `ticket-loop-subflow.md` §B new row |
| 2.1/2.2/2.3 — single / several-named / all targets | `clear-ticket-subflow.md` §A steps 2-4 |
| 3 — ambiguous references → numbered list, never auto-cleared | `clear-ticket-subflow.md` §A step 4.2 |
| 4 — clearing active ticket unsets active state, no auto-promotion | `clear-ticket-subflow.md` §C step 2 |
| 5 — confirm gate above 1, single target clears without extra confirm | `clear-ticket-subflow.md` §B (count-based) |
| 6 — next-iteration summary reflects updated context immediately | no new presentation logic needed — `ticket-loop-subflow.md` §C already reads live `$TICKET_CONTEXT` |
| 7 — logic in its own subflow file; TOC updated | `clear-ticket-subflow.md` (new file); `references-toc.md` (new row) |

No AC is partial or reinterpreted.

---

## Tests added

None — no automated test harness in this repo (markdown-procedure repo). Coverage is the structural grep checks and the plan's dry-run traces (plan §5), both reproduced above or traceable directly against the new subflow's text; every trace in plan §5 maps to an explicit branch in `clear-ticket-subflow.md` §A/§B/§C.

---

## Honest notes (optional)

`skills/references/breakdown-subflow.md` carries a pre-existing, uncommitted `<!-- TODO REVIEW -->` comment (about the Present step needing its own harness delivery) that predates this step and is unrelated to Clear. Left untouched and unstaged deliberately — it is someone else's in-flight annotation, not this step's concern, and `hb-task-step-review-address` is the designed mechanism for picking up `TODO REVIEW` comments from uncommitted changes.

---

## Follow-up raised / Deferred / Out of scope

- Push action — deferred to step 5 of this task, per ticket.md's explicit out-of-scope bullet.
- No persistence implications — Clear only mutates the in-conversation `$TICKET_CONTEXT` model; nothing under `.hb/` is touched by this step (beyond this execution summary and the conditional facts-store check, which produced no write).
- Auto-promoting a new active ticket after a clear — explicitly excluded by AC4; not left as a follow-up, a rule enforced by omission.
