# Step 0 Execution — Extract shared breakdown subflow

**Executed:** 2026-07-10T11-31-40-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** (committed in Step 6 below)
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

`hb-task-plan`'s inline gap-analysis / propose-confirm / per-step-creation
logic (previously `hb-task-plan.md:64-96`, Steps 6-8) is now a shared,
generalized subflow at `skills/references/breakdown-subflow.md`, parameterized
over a parent (with acceptance criteria) and a set of existing children —
ready for `hb-ticket-discuss`'s Breakdown action to become its second consumer
in a later step, without duplicating this logic. Scope held to pure
extraction: no new breakdown capability, no change to `hb-task-plan`'s
task→step mechanics beyond routing through the shared file. `hb-task-plan.md`
went from 10 numbered steps to 8 (old Steps 6-8 collapsed into one Step 6 that
sets a caller contract and injects the subflow); wording and control flow were
traced 1:1 against the pre-edit text (§0.2 of the plan) with no changes found.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/breakdown-subflow.md` | **new** — the shared subflow: opening blockquote + caller contract (`$PARENT_LABEL`, `$PARENT_CRITERIA`, `$CHILDREN`, `$MATERIALIZE_CHILD`), then lettered Sections A (gap analysis), B (no-gaps exit), C (propose-confirm loop), D (per-child create-confirm loop) |
| `skills/hb-task-plan.md` | **edit** — replaced old Steps 6-8 (gap analysis / prompt / create) with a single new Step 6 ("Breakdown via shared subflow") that sets the four caller-contract variables and injects the subflow; renumbered old Step 9 "Report" → Step 7 and old Step 10 "Prompt user" → Step 8, verbatim (no wording change); Steps 1-5 and the argument surface (lines 1-9) untouched |
| `skills/references/references-toc.md` | **edit** — added one row for `breakdown-subflow.md`, placed directly after the `interactive-ticket-subflow.md` row |

No dependency manifest or lockfile exists for this repo's skill layer — none affected. `hb-ticket-discuss.md`, `hb-task-step-add.md`, and `skills/scripts/` were not touched.

---

## Deviations from plan

None of substance. The subflow file, the `hb-task-plan.md` Step 6 rewiring, and the TOC row were implemented exactly as specified in the plan's §2-§4.

---

## Verification

| Check | Result |
|---|---|
| Structural check (`grep -n "^###" skills/hb-task-plan.md`) | 8 numbered steps (was 10) |
| New file shape (`head -20 skills/references/breakdown-subflow.md`) | Opens with `> **Subflow — ...**` blockquote and a `**Caller contract.**` paragraph naming all four `$VAR`s, matching `interactive-ticket-subflow.md:1-9`'s shape |
| TOC updated (`grep -n "breakdown-subflow" skills/references/references-toc.md`) | Exactly one row |
| Side-effect-free subflow (`grep -n "hb-sdk\|git " skills/references/breakdown-subflow.md`) | No matches — subflow issues no SDK/git calls itself |
| Scope check (`git status --short`) | Exactly the three planned files: `M skills/hb-task-plan.md`, `M skills/references/references-toc.md`, `?? skills/references/breakdown-subflow.md` |
| Per-AC wording/behavior trace (AC 5) | Gap report still lists uncovered task-level criteria with partial-coverage notes (Section A); no-gaps message preserved (Section B); confirm/decline gate folded into Section C's confirm/revise/decline loop per the plan's Design decision; drafts still use `ticket-template.md`'s three sections with the <300-line sizing guidance (Section D); materialization still routes through `hb-task-step-add --ticket <path> [--flavor <slug>]` via `$MATERIALIZE_CHILD` |

```
$ grep -n "^###" skills/hb-task-plan.md
29:### 1. Help check
33:### 2. Resolve task path
42:### 3. Load task-level ticket
47:### 4. Discover existing steps
59:### 5. Report missing step tickets
64:### 6. Breakdown via shared subflow
73:### 7. Report
78:### 8. Prompt user

$ git status --short
 M skills/hb-task-plan.md
 M skills/references/references-toc.md
?? skills/references/breakdown-subflow.md
```

---

## Acceptance-criteria status

All ACs from the plan's §7 traceability table are satisfied:

- AC 1.1-1.4: satisfied by Sections A-D of `breakdown-subflow.md`.
- AC 2: satisfied by the "Caller contract" block naming all four `$VAR`s.
- AC 3: satisfied by Section D's contract (subflow drafts to temp paths only; persistence always via caller-supplied `$MATERIALIZE_CHILD`) and confirmed by the empty `grep -n "hb-sdk\|git "` result.
- AC 4: satisfied by `hb-task-plan.md`'s new Step 6 (contract setup + injection).
- AC 5: satisfied — wording and prompts traced 1:1 against the pre-edit text; no user-facing behavior change.
- AC 6: satisfied by the new `references-toc.md` row.

---

## Tests added

None — per the plan's §5, this step touches only `.md` prompt content; no `hb_sdk` Python code path was added or changed, and the repo's only automated suite (`tests/skills/scripts/hb_sdk/`) has no analog for skill/subflow markdown. Verification was the manual structural read-through and wording trace documented above.

---

## Honest notes (optional)

None.

---

## Follow-up raised / Deferred / Out of scope

Per the plan's Out of scope: `hb-ticket-discuss` is not touched in this step — wiring it as the subflow's second consumer is deferred to a later step in this task. No change was made to `hb-task-plan`'s task→step scoping, step-numbering, or step-creation mechanics beyond routing through the shared subflow. No new MCP/source integrations.
