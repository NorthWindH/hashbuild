# Step 3 Execution — Breakdown ticket action

**Executed:** 2026-07-15T05-22-43-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending — *committed immediately after this summary is written*
**Result:** ✅ Complete — all acceptance criteria met, all structural checks pass

---

## Outcome

The **motivating case** now works: a user with an Epic-shaped ticket active in
`hb-ticket-discuss`'s loop can say "break this down" and the loop dispatches a
new **Breakdown ticket** action, reusing `breakdown-subflow.md`'s existing
gap-analysis/propose-confirm/per-child create-confirm logic exactly as written
— no duplicated logic. The **general result**: the Action Registry grows from
2 dispatchable actions (Load, Describe) plus Exit to 3 plus Exit, purely
additively. Scope boundary held: **additive only** — `breakdown-subflow.md`
itself, and Sections A/C/D/E of `ticket-loop-subflow.md`, and the Load/Describe/
Exit action subflows are all byte-for-byte unchanged (confirmed by `git diff
--stat` showing no output for each). Zero new `allowed-tools` grants were
needed. No automated build/test gate exists for this markdown-procedure repo;
structural grep/diff checks (below) are the applicable gates and all pass.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/breakdown-ticket-subflow.md` | **new** — full Breakdown action subflow: §A resolve target ticket (named/active/ambiguous), §B supply `$PARENT_LABEL`/`$PARENT_CRITERIA`/`$CHILDREN`/`$MATERIALIZE_CHILD` and Follow `breakdown-subflow.md`, §C materialize callback (appends child entry with additive `parent` field), §D compose return outcome |
| `skills/references/ticket-loop-subflow.md` | **edit** — one new Action Registry row (§B) for Breakdown ticket, inserted between Describe and Exit; Sections A, C, D, E untouched (confirmed by diff showing only the §B table hunk) |
| `skills/hb-ticket-discuss.md` | **edit** — `description:` and body-prose example-actions list both gain "breakdown" (two locations, matching how step-2 updated the same two spots for Load); `allowed-tools` and `Steps` section untouched (confirmed by diff showing only the two wording hunks) |
| `skills/references/references-toc.md` | **edit** — one new row for `breakdown-ticket-subflow.md`, positioned between the `describe-ticket-subflow.md` and `exit-ticket-loop-subflow.md` rows |

No dependency manifest or lockfile in this repo's skill layer; none touched.

---

## Deviations from plan

None of substance. The plan's design, wording, and section structure were
carried out verbatim — the new subflow file matches the plan's §2
specification exactly (same 4 lettered sections, same algorithm per section),
and all four integration points in §3 landed as specified.

---

## Verification

| Check | Result |
|---|---|
| Build | N/A — markdown-procedure repo, no build |
| Full test suite | N/A — no automated harness (confirmed by prior steps' execution summaries) |
| Static analysis / lint | N/A |
| Formatter | N/A |
| Scope check (only intended files changed) | confirmed — `git status --short` shows exactly the 4 files from plan §4 |

Per-check evidence (plan §5/§6):

```
--- Action Registry row ---
38:| Breakdown ticket | "break this down", "break down the login epic", "decompose this ticket into smaller ones" | `breakdown-ticket-subflow.md` |
--- #### count in new file ---
4
--- reference to breakdown-subflow (Follow, not re-derived logic) ---
(9 lines, incl. §B step 3's `Follow [...](breakdown-subflow.md)` — no inline
 gap-analysis/propose-confirm/create-confirm text duplicated)
--- breakdown-subflow.md diff (expect none) ---
(no output — untouched)
--- allowed-tools block (unchanged) ---
allowed-tools: >
  Write(//tmp/*)
  Write(//private/tmp/*)
  Read(//tmp/*)
  Read(//private/tmp/*)
  Edit(//tmp/*)
  Edit(//private/tmp/*)
  mcp__claude_ai_Atlassian_Rovo__getAccessibleAtlassianResources
  mcp__claude_ai_Atlassian_Rovo__getVisibleJiraProjects
  mcp__claude_ai_Atlassian_Rovo__getJiraProjectIssueTypesMetadata
  mcp__claude_ai_Atlassian_Rovo__searchJiraIssuesUsingJql
  mcp__claude_ai_Atlassian_Rovo__getJiraIssue
--- hb-ticket-discuss.md diff (only two wording hunks) ---
diff --git a/skills/hb-ticket-discuss.md b/skills/hb-ticket-discuss.md
-  actions (e.g. describe, load, exit) selectable via natural language. Makes
-  no .hb/ writes.
+  actions (e.g. describe, load, breakdown, exit) selectable via natural
+  language. Makes no .hb/ writes.
-Run a persistent, ... a menu of next actions (e.g. describe, load, exit) selectable via natural language.
+Run a persistent, ... a menu of next actions (e.g. describe, load, breakdown, exit) selectable via natural language.
--- TOC row ---
18:| `${CLAUDE_SKILL_DIR}/references/breakdown-ticket-subflow.md`  | Breakdown-ticket action for `hb-ticket-discuss`'s loop; dispatched from `ticket-loop-subflow.md`'s Action Registry. |
--- reuse guard (expect no matches) ---
(no output — no Bash/hb-sdk calls inlined in the new subflow)
--- non-regression diff (load/describe/exit-ticket-loop-subflow.md, expect none) ---
(no output — all three untouched)
--- overall git status --short ---
 M skills/hb-ticket-discuss.md
 M skills/references/references-toc.md
 M skills/references/ticket-loop-subflow.md
?? skills/references/breakdown-ticket-subflow.md
```

Per-AC checks: see Acceptance-criteria status below; each AC's satisfying
section is unambiguous from the file-by-file diff/content evidence above.

---

## Acceptance-criteria status

All 7 acceptance criteria (numbered per plan §7's traceability table) are
satisfied by the shipped file content:

| AC | Satisfied by |
|---|---|
| 1 — Breakdown action added, NL-selectable, defaults to active ticket, asks if ambiguous | Action Registry row (`ticket-loop-subflow.md` §B); `breakdown-ticket-subflow.md` §A |
| 2 — supplies `breakdown-subflow.md` with parent criteria + tracked children | `breakdown-ticket-subflow.md` §B |
| 2.1 — Parent = targeted ticket's Acceptance Criteria | §A step 4 |
| 2.2 — Children = context entries previously created as children, via a parent reference | §B step 1 (filter by `parent`); §C step 4 (field set at materialize time) |
| 3 — subflow's own gap/no-gaps/propose-confirm/per-child behavior reused, not re-implemented | §B step 3 (`Follow`, not inline duplication); `breakdown-subflow.md` diff empty |
| 4 — on child confirm, added to context with parent record, becomes active; previously active entry not removed | §C steps 3-4 (unset-then-append) |
| 5 — no-gaps → return to menu, nothing created | §D no-gaps branch; Failure/degradation contract |
| 6 — action's logic lives in its own subflow file; TOC updated | new file `breakdown-ticket-subflow.md`; TOC row added |

No AC is partial or reinterpreted.

---

## Tests added

None — no automated test harness in this repo (markdown-procedure skills).
Coverage is the structural grep/diff checks above plus the dry-run traces
already worked through during planning (plan §5); no new dry-run trace was
needed during execution since the shipped content matches the planned design
verbatim.

---

## Honest notes

None beyond what's already stated above — this was a small, self-contained,
purely additive change and nothing unexpected surfaced during execution.

---

## Follow-up raised / Deferred / Out of scope

- Load, Clear, and Push actions remain out of scope for this step — Clear and
  Push are this task's steps 4 and 5 respectively (per plan §8).
- Persisting the parent/child relationship to `.hb/` remains out of scope —
  it lives only in the loop's in-conversation `$TICKET_CONTEXT` model, per
  plan §8.
