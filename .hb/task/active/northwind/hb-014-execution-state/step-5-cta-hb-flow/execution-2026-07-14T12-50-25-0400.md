# Step 5 Execution — Route all skill "Prompt user" CTAs through /hb-flow

**Executed:** 2026-07-14T12-50-25-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending step-execute commit
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

`hb-task-step-plan.md`'s CTA (the motivating case: "run `/hb-task-step-execute
<step_ref>`") and the nine other `hb-*` skills' terminal "Prompt user"/"Notify
user" messages no longer hard-code a specific next `hb-*` command. All ten now
route through `/clear` + `/hb-flow`, matching the canonical tail specified in the
plan. Documentation-only change — no skill's control flow, SDK behavior, or
`hb-flow.md` itself changed. Lint/format gates clean (no-op on these Markdown
files, confirming no tracked Python was touched).

---

## What was built / changed

| File | Change |
|---|---|
| `skills/hb-task-plan.md` | **edit** — `### 10. Prompt user` blockquote tail only |
| `skills/hb-init.md` | **edit** — `### 4. Prompt user to create their first task`, first paragraph only; `/hb-status` paragraph and ticket-file explanation untouched |
| `skills/hb-task-archive.md` | **edit** — `### 4. Prompt user` blockquote tail only |
| `skills/hb-task-unarchive.md` | **edit** — `### 4. Prompt user` blockquote tail only |
| `skills/hb-task-step-review-init.md` | **edit** — `### 5. Notify user`, 3rd blockquote paragraph only; first two paragraphs untouched |
| `skills/hb-task-step-review-address.md` | **edit** — `### 10. Prompt user` blockquote, trailing clause only; leading "review is iterative" sentence untouched |
| `skills/hb-task-step-add.md` | **edit** — `### 5. Prompt user`, both branch blockquotes' tails; lead sentences and "edit `ticket.md`" instruction untouched |
| `skills/hb-task-step-execute.md` | **edit** — `### 9. Prompt user` blockquote, full replacement (no outcome-specific text to preserve) |
| `skills/hb-task-step-plan.md` | **edit** — `### 8. Prompt user` blockquote tail only |
| `skills/hb-task-create.md` | **edit** — `### 5. Prompt user`, both branch blockquotes' tails; lead sentences and "write a `ticket.md` first" instruction untouched |

`skills/hb-flow.md` — **unchanged**, confirmed by `git diff --stat` (no output).
No dependency-manifest, lockfile, or non-`skills/*.md` file changes.

---

## Deviations from plan

None of substance. The plan's per-file before/after text (§2) was applied
verbatim; every diff hunk matches exactly what §2 specified.

---

## Verification

| Check | Result |
|---|---|
| Scope check (only intended files changed) | confirmed — `git status --short` shows exactly the ten `skills/*.md` files as `M` |
| No hard-coded next-skill command remains in CTA sites | confirmed — remaining `grep` hits are outside the CTA blockquote (a "re-run this same command" self-reference in `hb-task-step-review-address.md`'s CTA, and unrelated non-CTA prose in `hb-task-step-review-init.md` and `hb-task-step-review-address.md`) |
| `/hb-flow` appears in every file's CTA | confirmed — `grep -c` returns `1` for all files except `hb-task-step-add.md` and `hb-task-create.md`, which return `2` (one per branch), matching plan expectation exactly |
| `hb-flow.md` untouched | confirmed — `git diff --stat -- skills/hb-flow.md` produced no output |
| Per-file diff review | confirmed — `git diff` on all ten files shows only the routing-tail lines changed; lead sentences and non-CTA asides are byte-identical before/after |
| Lint | `uv run ruff check` → All checks passed! |
| Formatter | `uv run ruff format --check` → 23 files already formatted |

Per-file diff excerpt (representative — `hb-task-step-execute.md`, the fullest
collapse):
```
-> Step executed. `/clear` this conversation, then: to start a code review, either run `/hb-task-step-review-address <step_ref>` directly (if you added `TODO REVIEW` comments, committed or uncommitted), or run `/hb-task-step-review-init <step_ref>` to create `review.md` and fill in concerns manually. To move to the next step, run `/hb-task-step-add <name>` then `/hb-task-step-plan`. When all steps are done, run `/hb-task-archive <name>` to close the task.
+> Step executed. `/clear` this conversation, then run `/hb-flow` to see what to do next.
```

---

## Acceptance-criteria status

All four ACs satisfied per the plan's §7 traceability table:
- AC1: every listed CTA rewritten to `/clear` + `/hb-flow` (verification step 3).
- AC2: outcome-specific lead sentences (`hb-task-step-add.md` / `hb-task-create.md`
  branch splits) and the `hb-task-step-review-address.md` "review is iterative"
  aside preserved verbatim (verification step 5, diff review).
- AC3: `hb-flow.md` not edited (verification step 4).
- AC4: no remaining hard-coded `hb-*` command as primary CTA in any of the ten
  files (verification step 2).

---

## Tests added

None — plan §5 confirmed no test asserts against the literal prose of these ten
CTA messages; this is a documentation/prompt-text-only change with no
executable surface to unit-test. Verification was by direct inspection (grep +
diff), captured above.

---

## Honest notes (optional)

None.

---

## Follow-up raised / Deferred / Out of scope

Per plan §8 and ticket: `hb-flow.md`'s own routing logic, any skill outside the
ten listed CTA sites, the installed copy at `~/.claude/skills/hb-*/`, and
`HB_FLOW_HOOK_COMMAND` are all unchanged and out of scope for this step.
