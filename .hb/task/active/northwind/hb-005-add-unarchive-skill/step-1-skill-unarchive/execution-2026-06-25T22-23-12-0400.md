# Step 1 Execution — Add hb-task-unarchive Skill File

**Executed:** 2026-06-25T22-23-12-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** TBD — *hb-005/step-1: add hb-task-unarchive skill file*
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

Before this step, `/hb-task-unarchive` would fail with "skill not found," leaving the archive/unarchive round-trip incomplete. After: `skills/hb-task-unarchive.md` exists and the skill resolves and executes `hb-sdk task unarchive`. Change is purely additive — one new file, no existing file touched.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/hb-task-unarchive.md` | **new** — mirrors `hb-task-archive.md`; four-step skill (help check, SDK call, commit, prompt); all existing skill/script/test files left unchanged |

No new dependency; manifests unchanged.

---

## Deviations from plan

None of substance. The file was created exactly as specified in §2 of the plan — frontmatter fields, four-step body, and output section match the spec verbatim.

---

## Verification

| Check | Result |
|---|---|
| File exists | pass |
| Frontmatter `name` field | `hb-task-unarchive` ✓ |
| Frontmatter `description` | `/hb-task-unarchive [--help] <author/task-id>` ✓ |
| `allowed-tools` mirrors archive skill | `Bash(${CLAUDE_SKILL_DIR}/scripts/hb-sdk *) Bash(git *)` ✓ |
| Four-step body present | Steps 1–4 confirmed ✓ |
| Scope check (only intended files changed) | confirmed — only `skills/hb-task-unarchive.md` untracked |

```
$ ls skills/hb-task-unarchive.md
skills/hb-task-unarchive.md

$ head -10 skills/hb-task-unarchive.md
---
name: hb-task-unarchive
description: >
  /hb-task-unarchive [--help] <author/task-id>

  Unarchive a task by moving its folder from `task/archive` to `task/active`.
allowed-tools: Bash(${CLAUDE_SKILL_DIR}/scripts/hb-sdk *) Bash(git *)
---

# hb-task-unarchive

$ git status --short
?? skills/hb-task-unarchive.md
```

---

## Acceptance-criteria status

All ACs from the plan's §7 traceability table are satisfied:

| AC | Status |
|---|---|
| 1 — `skills/hb-task-unarchive.md` exists alongside `hb-task-archive.md` | ✅ |
| 2 — frontmatter `description` = `/hb-task-unarchive [--help] <author/task-id>` | ✅ |
| 3 — `allowed-tools` mirrors `hb-task-archive` | ✅ |
| 4a — Help check delegates to `skill-help.md` | ✅ |
| 4b — Unarchive step calls `hb-sdk task unarchive`; surfaces stderr on error | ✅ |
| 4c — Commit follows `committing.md` (non-step) | ✅ |
| 4d — Prompt user confirms restoration; suggests `/hb-status` or `/hb-task-step-add` | ✅ |
| 5 — Output reports restored task path; errors surfaced verbatim | ✅ |

---

## Tests added

Skill files are markdown instruction documents consumed by the AI harness; no automated test suite applies. The 91 existing `hb-sdk` tests (from step-0) pass unchanged — no SDK code was modified.

---

## Follow-up raised / Deferred / Out of scope

None. Step-0 shipped the SDK subcommand; this step closes hb-005 with the skill file. Task is ready to archive.
