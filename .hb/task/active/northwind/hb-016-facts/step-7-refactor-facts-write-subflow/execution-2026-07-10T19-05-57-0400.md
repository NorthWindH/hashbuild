# Step 7 Execution — Extract shared `facts-write-subflow.md`

**Executed:** 2026-07-10T19-05-57-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending — see step 8 of this skill run
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

The duplicated facts-store read/compose/gate/write logic previously copy-pasted into
`hb-task-step-plan.md`, `hb-task-plan.md`, `hb-task-step-execute.md`, and
`hb-task-step-review-address.md` — 4 near-identical "early read" blocks and 4
near-identical "late compose/gate/write" blocks, whose STEP-6-REVIEW-3 wording had
already drifted across copies — is now a single canonical file,
`skills/references/facts-write-subflow.md`, with two lettered parts (Part A — Read,
Part B — Compose/gate/write) invoked via a caller-contract + prose-follow convention
matching `breakdown-subflow.md`. Scope boundary held: **pure refactor** — no change
to `hb-sdk facts read`/`facts write` CLI behavior, no change to `.hb/facts.md`
content, no change to any skill's step numbering or externally observable commit
structure. Gate status: existing `hb_sdk` facts test suite green (7/7), all
structural verification checks pass.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/facts-write-subflow.md` | **new** — Part A (early read) and Part B (compose/gate/write, with the three STEP-6-REVIEW-3 criteria baked in explicitly) per plan §2 |
| `skills/references/references-toc.md` | **edit** — one new row added for `facts-write-subflow.md`; all 19 existing rows unchanged |
| `skills/hb-task-step-plan.md` | **edit** — Step 3 → follow Part A; Step 6 → caller contract (`$CONTEXT_LABEL="drafting this plan.md"`, `$SELF_COMMIT=false`) + follow Part B. Step 7 (deferred commit) untouched |
| `skills/hb-task-plan.md` | **edit** — Step 3 → follow Part A; Step 8 → caller contract (`$CONTEXT_LABEL="gap analysis and breakdown"`, `$SELF_COMMIT=true`, `$COMMIT_ARGS`="mode task, only .hb/facts.md, --tag task-plan") + follow Part B, replacing the old inline steps 8.1–8.2 self-commit logic |
| `skills/hb-task-step-execute.md` | **edit** — Step 4 → follow Part A; Step 7 → caller contract (`$CONTEXT_LABEL="this execution"`, `$SELF_COMMIT=false`) + follow Part B. Step 8 (bundled commit) untouched |
| `skills/hb-task-step-review-address.md` | **edit** — sub-step 9a → follow Part A; sub-step 9f → caller contract (`$CONTEXT_LABEL="addressing this item"`, `$SELF_COMMIT=false`) + follow Part B. Sub-step 9g (bundled per-item commit) and the 9a–9g loop structure untouched |

No dependency-manifest or lockfile effects — all changes confined to `skills/**/*.md`.
No frontmatter (`allowed-tools`, `argument-hint`, etc.) changed in any of the four
skills. No `.hb/facts.md` content change.

---

## Deviations from plan

None of substance. The plan's design (§1–§2), file-by-file changes (§4), and
verification steps (§6) were followed exactly as specified, including the
caller-contract variable names (`$CONTEXT_LABEL`, `$SELF_COMMIT`, `$COMMIT_ARGS`)
and the per-caller values from §4's table.

---

## Verification

| Check | Result |
|---|---|
| `hb_sdk` facts CLI test suite (non-regression) | pass — 7/7 (`.venv/bin/python -m pytest tests/skills/scripts/hb_sdk/test_hb_sdk_facts.py -q`) |
| Scope check (only intended files changed) | confirmed — `git status --short` shows exactly the 6 files from plan §4 |
| No leftover duplicated inline sequence | confirmed |
| Every caller references the new subflow | confirmed |
| `references-toc.md` lists the new file | confirmed |
| `.hb/facts.md` content unchanged (AC5) | confirmed |

Non-regression test run:

```
.venv/bin/python -m pytest tests/skills/scripts/hb_sdk/test_hb_sdk_facts.py -q
.......                                                                  [100%]
7 passed in 1.02s
```

No-leftover-duplication check (expect all matches inside the new subflow only):

```
grep -rn "using judgement, based on what" skills/*.md skills/references/*.md
skills/references/facts-write-subflow.md:31:- using judgement, based on what `$CONTEXT_LABEL` revealed — ...
```

Every-caller-references-subflow check (expect all 4 listed):

```
grep -l "facts-write-subflow.md" skills/hb-task-step-plan.md skills/hb-task-plan.md skills/hb-task-step-execute.md skills/hb-task-step-review-address.md
skills/hb-task-step-execute.md
skills/hb-task-plan.md
skills/hb-task-step-plan.md
skills/hb-task-step-review-address.md
```

`references-toc.md` row check:

```
grep -n "facts-write-subflow.md" skills/references/references-toc.md
20:| `${CLAUDE_SKILL_DIR}/references/facts-write-subflow.md` | ...
```

AC5 byte-identical facts check (matches the "before" capture in plan §0):

```
hb-sdk facts read
# Facts Store

- `skills/hb-*.md` is the canonical skill source; `~/.claude/skills/hb-*/` is just the installed copy.
- hb-015/step-5: re-author Jira push logic deleted in step-1 from hb-ticket-discuss.md (recover via git show 7bd2c42).
- hb-ticket-discuss.md allowed-tools omits Read/WebFetch on purpose; don't re-add (rejected in hb-015/step-2).
- Subflows invoked at 2+ points in one skill use caller-contract+prose-follow (like breakdown-subflow), not `!cat`.
```

Scope check:

```
git status --short
 M skills/hb-task-plan.md
 M skills/hb-task-step-execute.md
 M skills/hb-task-step-plan.md
 M skills/hb-task-step-review-address.md
 M skills/references/references-toc.md
?? skills/references/facts-write-subflow.md
```

---

## Acceptance-criteria status

All 5 ACs satisfied per plan §7's traceability table:

- AC1 (new subflow file, generalized from all 8 duplicated blocks): satisfied — `skills/references/facts-write-subflow.md` created.
- AC2 (STEP-6-REVIEW-3 discipline baked into every write): satisfied — the three criteria (prefer dropping trivially re-derivable facts, keep facts <= 120 chars, add new facts only when they matter) are stated explicitly in Part B and apply on every invocation, not just during review.
- AC3 (all 4 skills use caller-contract + follow-reference, no inline prose): satisfied — verified by grep (no leftover duplicated phrase outside the subflow) and by direct read of each edited section.
- AC4 (no behavior regression — self-commit vs. deferred-commit preserved per caller): satisfied — `hb-task-plan.md` retains `$SELF_COMMIT=true` with its own `$COMMIT_ARGS`; the other three retain `$SELF_COMMIT=false` and their existing later bundled-commit steps, untouched.
- AC5 (no `.hb/facts.md` content change): satisfied — `hb-sdk facts read` output is unchanged from the plan's "before" capture (still 3 original facts plus the pre-existing 4th fact about subflow conventions, none touched by this step).

---

## Tests added

None — this is a documentation/skill-markdown refactor with no automated test
convention for skill structure (per plan §5/§8). The existing `hb_sdk` facts CLI
suite (`tests/skills/scripts/hb_sdk/test_hb_sdk_facts.py`, 7 tests) was required to
pass unchanged as a non-regression check on the untouched `facts.py` CLI, and did.

---

## Honest notes (optional)

None.

---

## Follow-up raised / Deferred / Out of scope

None — matches plan §8's out-of-scope list exactly (no `.hb/facts.md` content
change, no `hb-sdk facts` CLI change, no `facts-template.md` change, no skill
numbering/frontmatter change, no new automated test coverage).
