# Step 5 Execution — Document `facts.md` in `structure.md`

**Executed:** 2026-07-10T16-12-55-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending commit on `master` — *hb-016/step-5: (step-execute) document facts.md in structure.md*
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

`skills/references/structure.md` previously documented `.hb/`'s layout without any
mention of `facts.md`, even though hb-016 introduced it as a top-level sibling of
`task/`. The doc now lists `facts.md` in the top-level prose bullet list (noting it
is created/maintained via `hb-sdk facts write`) and shows it as a sibling of `task/`
in all three "Structure Examples" trees. Scope boundary held: purely additive — no
existing bullet, table row, or tree line was removed or reworded. No gates apply
(documentation-only change, no build/test/lint surface).

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/structure.md` | **edit** — added a `facts.md` prose bullet under the `.hb/` top-level list (between the "all files..." line and the `task/` bullet); added a `facts.md` tree entry as a sibling of `task/` at the `.hb/` root in all 3 example trees. Task/Step Name Format sections (lines 134+ pre-edit) left untouched. |

No dependency-manifest or lockfile effects — markdown-only change.

---

## Deviations from plan

None of substance. The plan's suggested tree ordering (`facts.md` before `task/`,
using `├──` for `facts.md` and `└──` for `task/`) was followed exactly in all three
trees.

---

## Verification

| Check | Result |
|---|---|
| Build | N/A — documentation-only, no build step |
| Full test suite | N/A — no `test_structure*` file exists under `tests/` |
| Static analysis / lint | N/A |
| Formatter | N/A |
| End-to-end run / artifact regeneration | N/A |
| Scope check (only intended files changed) | confirmed — `git status --short` shows only `skills/references/structure.md` modified |

Per-AC checks with actual output:

```
$ git diff skills/references/structure.md
--- a/skills/references/structure.md
+++ b/skills/references/structure.md
@@ -2,6 +2,8 @@
 
 - all files in `.hb` at top-level in project directory
 - inside `.hb/`:
+  - `facts.md`: persistent, project-level facts store; created/maintained via
+    `hb-sdk facts write`
   - task folders in `task/`
   - inside `task/` folder:
     - active task folders in `active/`
@@ -26,6 +28,7 @@
 
 ```
 .hb/
+├── facts.md
 └── task/
     ├── active/
     │   └── hasan/
@@ -62,6 +65,7 @@ Mixed — some steps with `step_extra`, some without:
 
 ```
 .hb/
+├── facts.md
 └── task/
     ├── active/
     │   └── hasan/
@@ -84,6 +88,7 @@ Multi-author, multiple active tasks:
 
 ```
 .hb/
+├── facts.md
 └── task/
     ├── active/
     │   ├── hasan/
```

All hunks are pure insertions — no line removed or reworded, confirming
non-regression (§0.2 of plan).

```
$ grep -c facts.md skills/references/structure.md
4
```
4 mentions: 1 prose + 3 tree entries, matching AC 3's expectation exactly.

---

## Acceptance-criteria status

All 3 ACs satisfied per the plan's §7 traceability table:
- AC 1: prose lists `facts.md` as a top-level file with a one-line purpose —
  confirmed, new bullet reads "`facts.md`: persistent, project-level facts store;
  created/maintained via `hb-sdk facts write`".
- AC 2: prose states the creator is `hb-sdk facts write` — confirmed, phrase
  appears verbatim in the same bullet.
- AC 3: all 3 example trees include a `facts.md` entry — confirmed via the grep
  count (4 total mentions) and the diff above showing one addition per tree.

---

## Tests added

None — documentation-only change, no executable behavior to lock down with tests.

---

## Honest notes (optional)

None.

---

## Follow-up raised / Deferred / Out of scope

None. Per plan §8, `facts.py`/`common.py`/other `hb-sdk` behavior,
`facts-template.md` content guidance, the Task/Step Name Format sections, and
other skills' Reference Files tables were all deliberately left untouched and
were not touched.
