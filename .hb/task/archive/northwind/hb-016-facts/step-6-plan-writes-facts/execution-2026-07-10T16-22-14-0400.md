# Step 6 Execution — Write-after facts in the planning skills

**Executed:** 2026-07-10T16-22-14-0400
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending — *hb-016/step-6: (step-execute) wire write-after facts into planning skills*
**Result:** ✅ Complete — all acceptance criteria met, dry-run trace verification clean

---

## Outcome

Both planning skills — `hb-task-step-plan.md` and `hb-task-plan.md` — now close the
read-before/write-after facts loop that `hb-task-step-execute.md` and
`hb-task-step-review-address.md` already had. Before this step, a fact discovered
while drafting a `plan.md` or during gap analysis/breakdown was silently dropped;
after this step, both skills re-read `.hb/facts.md`, correct/extend it by judgement,
and commit the change (`hb-task-step-plan.md` folds it into its existing plan.md
commit; `hb-task-plan.md` performs its own new task-level commit since it had no
pre-existing commit step). Documentation-only change — no application code, no new
public API. Scope held exactly as planned: only the two target files changed.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/hb-task-step-plan.md` | **edit** — inserted new step 6 "Update facts store" (read `$FACTS_AFTER`, compose by judgement against `facts-template.md` size guidance, write if changed); renumbered old step 6 (Commit) → 7, adding a clause to include `.hb/facts.md` in the commit "if it was changed in the previous step"; old step 7 (Prompt user) → 8, unchanged text; old step 8 (Record execution state) → 9, unchanged text. Steps 1–5 untouched. |
| `skills/hb-task-plan.md` | **edit** — inserted new step 8 "Update facts store" after step 7 (Breakdown via shared subflow): read `$FACTS_AFTER`, compose by judgement, and — since this skill has no pre-existing top-level commit step — perform its own commit (`committing.md`, mode `task`, `--tag task-plan`, staging only `.hb/facts.md`) when content changed, otherwise no-op both write and commit; old step 8 (Report) → 9, unchanged text; old step 9 (Prompt user) → 10, unchanged text. Steps 1–7 untouched. |

No dependency-manifest or lockfile changes. `.hb/facts.md` itself was not created or
modified by this step — it remains absent in this repo and is only ever written at
runtime by these skills once invoked going forward.

---

## Deviations from plan

None of substance. The edits match §2.1 and §2.2 of the plan verbatim in shape and
placement; wording was adapted slightly per skill (e.g. "what drafting this
`plan.md` revealed" vs. "what gap analysis and breakdown revealed") as the plan
itself specified.

---

## Verification

| Check | Result |
|---|---|
| Build | N/A — Markdown skill definitions, no build step |
| Full test suite | N/A — no application test suite covers skill Markdown |
| Static analysis / lint | N/A |
| Formatter | N/A |
| End-to-end run / artifact regeneration | N/A — verified by dry-run trace, per plan §5 |
| Scope check (only intended files changed) | confirmed — `git status --short` shows only `skills/hb-task-plan.md` and `skills/hb-task-step-plan.md` modified |

Scope check output:

```
 M skills/hb-task-plan.md
 M skills/hb-task-step-plan.md
```

Sequential step numbering confirmed by reading both files end to end:
- `hb-task-step-plan.md`: steps 1–9, no gaps or duplicates; step 7 (Commit) now
  reads "...including `$STEP_PATH/plan.md` and `.hb/facts.md` if it was changed in
  the previous step; pass `--tag step-plan`".
- `hb-task-plan.md`: steps 1–10, no gaps or duplicates; new step 8 states its own
  commit "mode `task`, `--tag task-plan`" staging only `.hb/facts.md`.

Both new steps cite `references/facts-template.md` and the identical size guidance
figures (`<= 100 lines` target, `1000` hard max, `<= 120` chars/line) used in the
existing `hb-task-step-execute.md` step 7 and `hb-task-step-review-address.md` step
9f write-after steps.

**Per-AC checks:**
- AC1 (`hb-task-step-plan.md` write-after): new step 6, `skills/hb-task-step-plan.md:71-87`; folded into commit at `skills/hb-task-step-plan.md:89-91`.
- AC2 (`hb-task-plan.md` write-after): new step 8, `skills/hb-task-plan.md:83-100`.
- AC3 (facts changes land in a real commit): step-plan folds into its existing renumbered Commit (step 7); task-plan's step 8 performs its own commit — both via `committing.md`.
- AC4 (missing/empty store never errors): both new steps reuse the identical `hb-sdk facts read` call and "may be empty" wording already proven safe in step-1 of this task; confirmed live — `hb-sdk facts read` currently returns empty in this repo and neither skill's new step branches on that being an error.

**Non-regression:** `skills/hb-task-step-execute.md` and
`skills/hb-task-step-review-address.md` were not read or modified during this step
(only referenced from the plan for shape); their existing write-after steps remain
the reference implementation.

---

## Acceptance-criteria status

All four ACs (§7 of `plan.md`) satisfied as traced above. No partial or
interpreted criteria.

---

## Tests added

None — no application test suite exists for Markdown skill definitions in this
project; verification is by dry-run trace against the edited step text (see
Verification above), matching how step-2 and step-4 of this task were verified.

---

## Honest notes (optional)

`.hb/facts.md` does not exist in this repo yet, so the new write-after steps in
both skills have not been exercised against a real non-empty facts file — only
against the established "empty read → no-op or fresh small file" contract. First
real content will land the first time either skill runs against a ticket/breakdown
that surfaces a durable fact.

---

## Follow-up raised / Deferred / Out of scope

None raised. Per ticket, changes to `breakdown-subflow.md`, `$MATERIALIZE_CHILD`,
and `hb-task-step-add.md` commit behavior remain explicitly out of scope and were
not touched.
