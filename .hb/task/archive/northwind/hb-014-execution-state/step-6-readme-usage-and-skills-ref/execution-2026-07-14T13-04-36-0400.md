# Step 6 Execution — README usage guidance + skills reference sync

**Executed:** 2026-07-14T13:04:36-04:00
**Plan:** `./plan.md` · **Ticket:** `./ticket.md`
**Commit / Branch:** pending — committed in the next step
**Result:** ✅ Complete — all acceptance criteria met, gates green

---

## Outcome

`README.md` (a symlink to `skills/references/README.md`, the canonical
target) now names `/hb-flow` as the day-to-day entry point and lists all 13
shipped skills. Before: the Skills table had 11 rows and never mentioned
`hb-flow` or `hb-ticket-discuss`; Getting started jumped straight to "1.
Initialize your project" with no pointer to `hb-flow`. After: 13 table rows
(11 → 13) and a new lead-in paragraph before step 1 recommending `/hb-init`
then `/hb-flow`. Scope boundary held: additive only — the existing numbered
walkthrough (steps 1–6, Unarchive, Check status), Installation, Lifecycle
diagram, and File structure sections are byte-for-byte unchanged.

---

## What was built / changed

| File | Change |
|---|---|
| `skills/references/README.md` (canonical target of the `README.md` symlink) | **edit** — added `/hb-flow` row (top of Skills table) and `/hb-ticket-discuss` row (bottom of Skills table); inserted a 5-line lead-in paragraph immediately before `### 1. Initialize your project` recommending `/hb-init` → `/hb-flow`. All other content (Installation, Lifecycle diagram, File structure, the numbered Getting started steps 1–6, Unarchive/Check-status subsections) left untouched. |

No dependency-manifest or lockfile changes. No `skills/*.md`, script, or
`.hb/` behavior changes, per plan scope.

One deviation from the plan's file-path framing, noted below.

---

## Deviations from plan

**What the plan said:** edit `README.md` directly (§2 of plan.md refers to
`README.md` throughout).

**What was done instead:** edited `skills/references/README.md`, the real
file `README.md` symlinks to (`README.md -> skills/references/README.md`,
confirmed via `readlink -f`). The `Edit` tool refused to write through the
symlink and named the real target explicitly.

**Why:** repo-level detail not visible during planning; the plan's content
prescriptions (table rows, lead-in wording, insertion points) apply
identically regardless of which path receives the write.

**Why the result is still correct:** `README.md` is a symlink, so reading it
via the symlink path reflects the edit — verification commands in §4 of the
plan were re-run against `README.md` and pass identically to what they'd show
against the real target. No content deviated from plan.md §2.1/§2.2 — table
row text and lead-in paragraph text were copied verbatim from the plan.

No other deviations of substance.

---

## Verification

| Check | Result |
|---|---|
| Build | N/A — documentation-only change, no build step |
| Full test suite | N/A — no test suite applies to a Markdown-only change |
| Static analysis / lint | N/A — not applicable to Markdown |
| Formatter | N/A — not applicable to Markdown |
| End-to-end run / artifact regeneration | N/A |
| Scope check (only intended files changed) | confirmed — `git diff` shows only `skills/references/README.md`, two additive hunks |

**AC1 check** — lead-in names `/hb-init` first, `/hb-flow` second, framing
`/hb-flow` as the standard entry point:

```
$ grep -n "hb-flow\|hb-ticket-discuss" README.md
70:| `/hb-flow`                     | Report state and recommended next action, then route to the right skill from a natural-language reply |
82:| `/hb-ticket-discuss`           | Draft standalone tickets in a multi-turn loop (not attached to a task/step) |
89:run `/hb-flow` whenever you're not sure what to do next — it reports where
91:reply. The steps below spell out what `/hb-flow` is doing under the hood, and
```

**AC2 check** — every `skills/*.md` file has a corresponding Skills table row:

```
$ ls skills/*.md | wc -l
13
$ sed -n '66,83p' README.md | grep -c '^| `/'
13
```

**Table render / row placement check** (plan §4.2):

```
$ sed -n '66,84p' README.md
## Skills

| Skill                          | What it does                                                         |
| ------------------------------ | -------------------------------------------------------------------- |
| `/hb-flow`                     | Report state and recommended next action, then route to the right skill from a natural-language reply |
| `/hb-init`                     | Initialize `.hb/` in your project (idempotent, run once per project) |
...
| `/hb-task-unarchive`           | Restore an archived task back to active                              |
| `/hb-ticket-discuss`           | Draft standalone tickets in a multi-turn loop (not attached to a task/step) |
```

`hb-flow` sits first (right after the header separator), `hb-ticket-discuss`
sits last — matches plan §2.1.

**Diff scope check** (plan §4.3) — `git diff README.md skills/references/README.md`
shows exactly two additive hunks: one new Skills-table row pair (top +
bottom) and one new lead-in paragraph before "### 1. Initialize your
project." No other line changed.

---

## Acceptance-criteria status

Both ACs fully satisfied:

- **AC1** — Getting started now opens with a lead-in naming `/hb-init` then
  `/hb-flow` as the recommended day-to-day entry point (README.md:88–92),
  satisfied per plan §5.
- **AC2** — Skills table row count matches `skills/*.md` file count (13 = 13),
  satisfied per plan §5.

---

## Tests added

None — documentation-only change, no test suite applies (plan §3).

---

## Honest notes

None.

---

## Follow-up raised / Deferred / Out of scope

None beyond what plan.md §6 already scoped out (rewriting the numbered
walkthrough; changes to `skills/*.md`, install scripts, or `.hb/` behavior;
adding `hb-ticket-discuss` to the numbered lifecycle walkthrough or Lifecycle
diagram).
